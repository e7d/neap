# .scrutinizer.yml
build:
    environment:
        php:
            version: 7.0
    tests:
        override:
            -
                command: ./module/run-tests.sh
                coverage:
                    file: ./build/logs/clover.xml
                    format: php-clover
