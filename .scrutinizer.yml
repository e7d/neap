# .scrutinizer.yml
build:
    environment:
        php:
            version: 7.0
    tests:
        override:
            -
                command: ./api/module/run-tests.sh
                coverage:
                    file: ./api/build/logs/clover.xml
                    format: php-clover
