server {
    # Listening interfaces
    listen 80;
    listen 443 ssl;

    # Hostname
    server_name db.neap.dev;

    # Root folder
    root neap/db;
    index.php;

    # SSL settings
    include snippets/ssl.conf;

    # PHP Pear
    include snippets/php.conf;
}
