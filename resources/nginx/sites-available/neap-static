server {
    # Listening interfaces
    listen 80;
    listen 443 ssl;

    # Root folder
    root neap/static;
    index no;

    # Hostname
    server_name static.neap.dev;

    # SSL settings
    include snippets/ssl.conf;
}
